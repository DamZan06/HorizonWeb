You are performing a focused corrective refactor on the current HORIZON repository.

DO NOT redesign the website visually.

The previous refactor was only partially completed.

There are now several concrete architectural problems that must be fixed before any more features are added.

The absolute priorities are:

1. FIX THE NAVIGATION FOR REAL.
2. FIX THE LIVE MAP AND ITS LOADING PERFORMANCE.
3. COMPLETE THE PARTIALLY EMPTY MODULAR ARCHITECTURE.
4. REMOVE UNUSED DEPENDENCIES.
5. VERIFY THE DEPLOYED GITHUB PAGES VERSION AFTER CHANGES.

---

# 1. IMPORTANT — THE NAVIGATION WAS NOT ACTUALLY CHANGED

The current public website and repository still use:

HOME
LIVE
THE CHALLENGE
ROUTE
JOURNEY
REPLAY
ABOUT

This is the old structure.

Do not leave it like this.

Replace the GLOBAL navigation everywhere with exactly:

HOME
LIVE
PROJECT
JOURNEY
REPLAY

That means:

Home:
`index.html`

Live:
`live.html`

Project:
`project.html`

Journey:
use the current canonical Journey page

Replay:
`replay.html`

Remove global navbar entries:

THE CHALLENGE
ROUTE
ABOUT

Those belong inside Project.

---

# 2. PROJECT PAGE INTERNAL NAVIGATION

Inside `project.html`, optionally create a small secondary anchor navigation:

OVERVIEW
TEAM
WHY HORIZON
ROUTE
CHALLENGE
HOW IT WORKS

This is not part of the global navbar.

---

# 3. CHANGE THE NAVBAR IN EVERY PAGE

Search every HTML file.

The navbar must NOT be manually left different between pages.

Update at least:

index.html
live.html
project.html
gallery/journey page
replay.html

Also inspect:

dashboard.html
diary.html
progress.html
timeline.html
admin.html

if they contain the public header.

The final public navbar must be consistent.

---

# 4. CENTRALIZE NAVIGATION DEFINITION

Do not manually maintain the navigation structure independently in every HTML page.

Create something like:

`js/ui/navigation.js`

Define the navigation once:

```js
const NAV_ITEMS = [
    { key: 'home', href: 'index.html' },
    { key: 'live', href: 'live.html' },
    { key: 'project', href: 'project.html' },
    { key: 'journey', href: 'gallery.html' },
    { key: 'replay', href: 'replay.html' }
];
```

The exact implementation may differ.

The important requirement is:

ONE canonical navigation definition.

---

# 5. ACTIVE PAGE

Determine the active page from:

`body[data-page]`

or pathname.

Automatically set:

`aria-current="page"`

Do not hard-code it inconsistently across HTML files.

---

# 6. MOBILE NAVIGATION

Mobile menu must contain only:

HOME
LIVE
PROJECT
JOURNEY
REPLAY
LANGUAGE

No:

THE CHALLENGE
ROUTE
ABOUT

Keep the existing accessible open/close behavior.

---

# 7. CRITICAL MAP ARCHITECTURE PROBLEM

The repository currently contains:

`js/map.js`

but it is EMPTY.

It also contains:

`js/firebase.js`

but it is EMPTY.

And:

`js/ui.js`

is EMPTY.

This is not an acceptable final architecture.

Do not create placeholder modules and then continue using legacy logic elsewhere.

Implement the modules or delete them.

---

# 8. LIVE.HTML CURRENT SCRIPT PROBLEM

The Live page currently loads:

Leaflet
leaflet-gpx
Chart.js
config.js
i18n.js
status.js
app.js
script.js

But it does NOT explicitly load:

map.js
firebase.js

This means the modular architecture is incomplete.

Fix this.

---

# 9. TARGET LIVE PAGE ARCHITECTURE

Use a clean dependency flow.

Recommended:

config.js
↓
firebase.js
gpx.js
↓
map.js
stats.js
↓
live.js

Global:

i18n.js
navigation.js

Conceptually:

```text
live.html

→ config
→ i18n
→ navigation
→ firebase service
→ GPX service
→ map
→ stats
→ live page controller
```

Do not initialize Live functionality through a generic old bootstrap.

---

# 10. CREATE A PAGE-SPECIFIC LIVE CONTROLLER

Create:

`js/pages/live.js`

or equivalent.

Its only responsibility is coordinating the Live page.

Example responsibility:

1. initialize Leaflet
2. display map immediately
3. load static route
4. fetch last known live position
5. update map marker
6. update statistics
7. start ONE live refresh loop

Do not put:

translations
navigation
gallery
replay
admin

inside this file.

---

# 11. THE MAP MUST APPEAR IMMEDIATELY

This is extremely important.

Currently the map feels like it takes forever to appear.

The visual map must NOT wait for:

Firebase
GPX parsing
statistics
weather
live position
replay data

before displaying.

Correct loading order:

### STEP 1 — IMMEDIATE

Create Leaflet map.

Add base tile layer.

User should see a usable map almost immediately.

### STEP 2

Load the HORIZON route.

### STEP 3

Load live position.

### STEP 4

Load secondary statistics/weather/etc.

The base map must not be blocked by expedition data.

---

# 12. DO NOT BLOCK MAP INITIALIZATION ON FIREBASE

Bad pattern:

```js
await fetchFirebase();
await loadGpx();
initializeMap();
```

Do NOT do this.

Better:

```js
initializeMap();

Promise.allSettled([
    loadRoute(),
    loadLivePosition(),
    loadStats()
]);
```

Use graceful independent loading.

A failed Firebase request must not prevent the map from appearing.

---

# 13. DO NOT BLOCK MAP ON GPX

Likewise:

the map itself should exist before GPX parsing completes.

Show base tiles immediately.

Then add route when ready.

---

# 14. REPLACE LEAFLET-GPX IF IT IS THE BOTTLENECK

Audit actual route size.

If `Horizon.gpx` contains thousands or tens of thousands of GPX points, parsing the complete XML with `leaflet-gpx` every page load may be unnecessarily expensive.

For the public Live map, create a preprocessed route file.

Recommended:

`data/horizon-route.geojson`

or:

`data/horizon-route.json`

generated once from the GPX.

The GPX remains the authoritative source.

But browser runtime should not repeatedly parse a huge XML file if a lightweight representation is sufficient.

---

# 15. GPX REMAINS SOURCE OF TRUTH

Do NOT manually simplify the HORIZON route.

Create a preprocessing utility in:

`tools/`

For example:

`tools/build-route.js`

or Python equivalent.

It should:

read:
`Horizon.gpx`

produce:
`horizon-route.geojson`

Preserve the real geometry.

If simplification is necessary for performance, use a documented geometric simplification algorithm with a very low tolerance.

Do not visually alter the route.

---

# 16. CREATE TWO ROUTE REPRESENTATIONS IF USEFUL

Recommended:

FULL route:
for detailed map / replay

LIGHT route:
for homepage overview and initial live-map rendering

For example:

`horizon-route-full.geojson`

`horizon-route-lite.geojson`

The lite route should preserve all meaningful geographic bends.

Do not reduce it to an artistic approximation.

---

# 17. MAP PERFORMANCE TARGET

On a normal broadband connection:

the empty/base Leaflet map should become visible essentially immediately after page layout.

Route rendering should follow quickly.

Live position may appear later.

The visitor should NEVER stare at a blank map container waiting for Firebase.

---

# 18. ADD A REAL MAP LOADING STATE

Inside the map container initially show:

`LOADING MAP…`

But remove it as soon as Leaflet and the base tile layer are initialized.

Separate state for route:

`Loading route…`

if needed.

Separate state for live signal:

`Connecting to live position…`

Do not use one giant loading state for everything.

---

# 19. REMOVE CHART.JS FROM LIVE.HTML IF UNUSED

The current Live page loads:

`https://cdn.jsdelivr.net/npm/chart.js`

Audit whether live.html actually creates a chart.

If not:

REMOVE CHART.JS FROM LIVE.HTML.

Do not load several hundred KB of JavaScript for no reason.

Only load Chart.js on pages that genuinely use charts.

---

# 20. PIN LEAFLET VERSION

The Live page currently uses unversioned:

`https://unpkg.com/leaflet/dist/leaflet.css`

and:

`https://unpkg.com/leaflet/dist/leaflet.js`

Pin an explicit known-working version.

For example use a stable tested Leaflet version consistently for CSS and JS.

Do not use `/leaflet/dist/...` without a version.

---

# 21. CONSIDER HOSTING LEAFLET LOCALLY

Because HORIZON needs to remain reliable during a real expedition, consider storing the required Leaflet production assets inside the repository.

This eliminates a third-party CDN as a critical dependency.

If local hosting is chosen:

keep only the minified production Leaflet JS/CSS and necessary images.

Do not commit an entire node_modules directory.

---

# 22. REMOVE LEAFLET-GPX IF PREPROCESSING ROUTE

If the route is converted to GeoJSON ahead of time:

remove:

`leaflet-gpx`

from the Live page.

Leaflet can render GeoJSON directly.

This removes another dependency and XML parser.

---

# 23. TILE PROVIDER

Audit the current tile provider.

Choose one reliable tile layer.

Do not create several tile layers immediately if only one is visible.

Additional terrain/satellite layers should be initialized lazily only when the user selects them.

Base map startup should require only ONE tile layer.

---

# 24. MAP TILE LOADING EXPERIENCE

Add listeners for:

`loading`

`load`

`tileerror`

Do not leave a permanently blank map if tiles fail.

If individual tiles fail:

keep the route and UI available.

Show a subtle message:

`Map background temporarily unavailable.`

---

# 25. FIT ROUTE ONLY ONCE

Do not repeatedly call:

`fitBounds()`

every time live data updates.

Initial behavior:

if live data exists:
choose appropriate initial viewport.

Otherwise:
fit the HORIZON route.

After the user manually moves/zooms the map:

do not automatically steal control from them every update.

---

# 26. LIVE MARKER UPDATE

Create the live marker once.

On subsequent updates:

use:

`marker.setLatLng(...)`

Do NOT:

remove marker
create marker
re-add layers
re-render entire route

every refresh.

---

# 27. ROUTE LAYER UPDATE

The planned route is static.

Load it ONCE.

Do not fetch or parse it every 8 seconds.

---

# 28. COMPLETED ROUTE

If completed/remaining route visualization is needed:

do not rebuild the entire GPX layer unnecessarily on every GPS update.

Use efficient precomputed route indexing or split the polyline only when progress materially changes.

---

# 29. FIREBASE SERVICE

Implement:

`js/services/firebase.js`

or use the existing `js/firebase.js`.

It should own:

Firebase endpoint
fetch logic
timeout
errors
normalization
latest point retrieval

The map must not know Firebase URLs.

The map receives normalized coordinates.

---

# 30. NETWORK TIMEOUT

Do not allow live-data requests to hang indefinitely.

Use:

`AbortController`

with a sensible timeout.

For example approximately:

5–8 seconds

depending on expected Firebase behavior.

If live data times out:

use last known valid value.

Do not block the rest of the UI.

---

# 31. CACHE STATIC ROUTE

The route is static.

Allow normal browser caching.

Do not append constantly changing query parameters to the route file.

Use versioning only when the route genuinely changes.

---

# 32. CACHE LAST KNOWN LIVE POSITION

Store the latest valid live point locally.

For example:

`localStorage`

or session memory.

On reload:

show the last known position immediately with a label:

`LAST KNOWN POSITION`

Then replace it when fresh data arrives.

This makes the Live page feel significantly faster during poor connections.

Do not present cached data as fresh/live.

---

# 33. PRIORITIZE ABOVE-THE-FOLD ASSETS

The Live page needs:

navigation
basic CSS
Leaflet
map container

before secondary content.

Do not block first map paint with:

weather
charts
gallery
translations not required immediately
admin code

---

# 34. DO NOT LOAD WEATHER BEFORE MAP

Weather is secondary.

Load it after:

map
route
live point

or lazily when its section becomes relevant.

---

# 35. MAP MODULE RESPONSIBILITY

`map.js` should implement ONLY map behavior.

It may contain:

createMap
addBaseLayer
addRoute
setLivePosition
setUserPosition
fitInitialView
toggleFullscreen
destroyMap

It should NOT contain:

Firebase fetch
translations
navigation
admin
general DOM content
weather

---

# 36. MAP CONTROLS MODULE

If map controls grow larger, separate:

`map-controls.js`

Responsibilities:

my location
center live
fullscreen
tooltips / accessibility labels

---

# 37. GEOLOCATION MUST BE ON DEMAND

Do not call browser geolocation on page load.

Only call it after:

MY LOCATION

is pressed.

This avoids unnecessary permissions and startup delay.

---

# 38. MAP FULLSCREEN

When entering or leaving fullscreen:

call:

`map.invalidateSize()`

after layout is stable.

Do not recreate the map.

---

# 39. RESIZE

Use a lightweight ResizeObserver if needed.

Do not repeatedly call `invalidateSize()` in a high-frequency resize loop.

Debounce if necessary.

---

# 40. CURRENT LIVE.HTML HAS UNUSED OR MISALIGNED ARCHITECTURE

Audit these current files:

`js/map.js`
`js/firebase.js`
`js/ui.js`
`js/charts.js`
`js/stats.js`
`js/weather.js`

Some are empty or not actually loaded.

For every file choose exactly ONE:

A. implement and use it

or

B. delete it.

No empty architectural placeholder files.

---

# 41. REMOVE GENERIC script.js BOOTSTRAP IF NO LONGER NEEDED

Current root `script.js` is only a small compatibility bootstrap.

Once page-specific initialization works properly:

determine whether it is still needed.

Prefer:

explicit page scripts

instead of:

poll every 25ms until some runtime global appears.

Do not retain compatibility code forever without reason.

---

# 42. DO NOT POLL FOR INITIALIZATION

Avoid patterns such as:

```js
setInterval(..., 25)
```

waiting for a global object to appear.

Script ordering should be explicit.

If dependencies are loaded correctly, initialization should be deterministic.

---

# 43. USE DEFER OR MODULES

Prefer either:

`defer`

with clear dependency ordering

or proper:

`type="module"`

imports.

A good architecture would use ES modules if compatible with the static GitHub Pages deployment.

Example:

```html
<script type="module" src="js/pages/live.js"></script>
```

Then:

```js
import { config } from '../core/config.js';
import { createMap } from '../features/map/map.js';
```

This is cleaner than large numbers of `window.Horizon...` globals.

If converting to modules would risk functionality, do it incrementally.

---

# 44. NAVBAR IMPLEMENTATION MUST BE VISIBLE IN SOURCE

This requirement is important because the previous task claimed to simplify navigation but the HTML remained unchanged.

After refactor, verify that public `index.html` and `live.html` no longer contain:

`The challenge`

`Route`

`About`

inside `.main-nav`.

They must contain:

Home
Live
Project
Journey
Replay

This is a PASS/FAIL requirement.

---

# 45. VERIFY GITHUB PAGES AFTER PUSH

After implementation:

check the actual deployed URLs:

`https://damzan06.github.io/HorizonWeb/`

and

`https://damzan06.github.io/HorizonWeb/live.html`

Do not only inspect local files.

Verify that the public page contains the new navbar.

---

# 46. CACHE BUSTING

The repository currently uses old-looking asset version strings such as:

`style.css?v=20260725d`

and script versions from different dates.

Standardize this.

Do not manually invent unrelated cache versions per script indefinitely.

Use one deploy version constant or update versions consistently when files change.

Alternatively use GitHub Pages caching behavior appropriately without excessive query strings.

---

# 47. PERFORMANCE MEASUREMENT

Use Chrome DevTools Network on `live.html`.

Measure:

HTML
CSS
Leaflet JS
Leaflet CSS
GPX / GeoJSON
Firebase
tiles
other JS

Report:

number of requests
transferred KB/MB
DOMContentLoaded
Load
largest slow requests

Do not simply say:

"performance improved."

Provide actual measurements.

---

# 48. MAP PERFORMANCE MEASUREMENT

Measure separately:

T0:
navigation begins

T1:
map container painted

T2:
Leaflet base map initialized

T3:
first tile visible

T4:
route visible

T5:
live marker visible

The key goal is:

T2 and T3 should NOT depend on T4 or T5.

---

# 49. SIMULATE SLOW NETWORK

Test Live page using Chrome DevTools:

Fast 3G / Slow 4G

Verify:

navbar appears quickly
stats skeleton appears
map container appears
base map does not wait for Firebase
route loads independently
site remains usable

This is particularly important because followers may open HORIZON using mobile data.

---

# 50. TEST FIREBASE FAILURE

Temporarily simulate Firebase failure.

The page must still display:

navbar
base map
planned HORIZON route
static project information

Only live point/statistics should degrade.

Show:

`LIVE DATA TEMPORARILY UNAVAILABLE`

Do not show an endless spinner.

---

# 51. TEST GPX/ROUTE FAILURE

Simulate route file failure.

The page must still display:

base map
live position if available
statistics

Show:

`PLANNED ROUTE COULD NOT BE LOADED`

Do not break the whole page.

---

# 52. TEST TILE FAILURE

Simulate tile server failure.

The route and live marker should still remain technically available against a neutral map background where possible.

Do not crash.

---

# 53. REMOVE UNUSED FILES AFTER REFACTOR

Do not leave:

empty map.js
empty firebase.js
empty ui.js

or old compatibility files.

A clean repository is part of this task.

---

# 54. TARGET PUBLIC JS STRUCTURE

A good final structure would be approximately:

```text
js/
  core/
    config.js
    state.js

  services/
    firebase.js
    route.js
    storage.js

  map/
    map.js
    controls.js

  ui/
    navigation.js
    i18n.js
    formatting.js

  pages/
    home.js
    live.js
    project.js
    journey.js
    replay.js
```

Do not follow this mechanically if another clean structure fits better.

But there must be clear ownership.

---

# 55. PERFORMANCE PRIORITY ORDER

Fix performance in this order:

1. map startup dependency chain
2. unnecessary external scripts
3. GPX parsing
4. live-data fetch strategy
5. tile loading
6. duplicate initialization
7. secondary UI

Do NOT start by minifying 2 KB JavaScript files while a huge GPX/parser/network chain blocks the map.

---

# 56. MAP UX TARGET

When a visitor opens Live:

within the first moment they should see:

HORIZON header
current state
stats placeholder
map surface

Then progressively:

map tiles
planned route
last/current position
stats

The interface should progressively enhance.

It must not appear frozen.

---

# 57. FINAL PASS/FAIL CHECKLIST

The task is NOT complete unless:

* navbar shows exactly HOME / LIVE / PROJECT / JOURNEY / REPLAY
* public GitHub Pages shows the new navbar
* map.js is implemented or removed
* firebase.js is implemented or removed
* ui.js is implemented or removed
* Live no longer loads unused Chart.js
* Leaflet has a pinned version
* map appears before Firebase response
* route loads only once
* live marker updates without recreating map
* GPX parsing is optimized/preprocessed if needed
* Firebase failure does not block map
* no empty architectural placeholder files remain
* no 25ms initialization polling remains unless rigorously justified
* public Live page is tested on throttled mobile network

---

# FINAL REPORT

After implementation give me:

1. OLD navbar
2. NEW navbar
3. files changed for navigation
4. reason previous navbar change did not happen
5. old map initialization sequence
6. new map initialization sequence
7. map.js final responsibility
8. firebase.js final responsibility
9. whether GPX is parsed at runtime
10. route file size before/after
11. Chart.js removed from Live: yes/no
12. Leaflet version
13. external dependencies remaining
14. time to first map tile
15. time to route visible
16. time to live marker visible
17. total Live network transfer
18. empty files removed
19. legacy compatibility code removed
20. remaining bottlenecks

Do NOT report completion without checking the deployed GitHub Pages website.
