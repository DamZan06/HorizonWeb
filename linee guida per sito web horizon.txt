# HORIZON — COMPLETE VISUAL, FUNCTIONAL AND LANGUAGE RESTORATION

You are working on the complete HORIZON website.

Live site:

https://damzan06.github.io/HorizonWeb/

Repository:

https://github.com/DamZan06/HorizonWeb

The website is currently cleaner than before, but the latest refactor has removed too much of its visual identity and several features are still incomplete or inconsistent.

Your task is to restore HORIZON as a **premium, cinematic, Alpine expedition website**, while preserving the improved code architecture.

This is NOT a request to redesign everything from scratch.

Keep the current structure where it is good.

Fix what is broken, restore what was lost, improve the styling, and test the complete site.

---

# ABSOLUTE COMPLETION RULE

DO NOT STOP after editing files.

DO NOT tell me the task is finished because the code compiles.

DO NOT stop after fixing only the issues explicitly mentioned here.

You may only declare completion when:

* every page works;
* every language works;
* every visible string is translated;
* the countdown works;
* the map loads;
* Firebase simulation data is visible;
* the visual identity is restored;
* participant images are correctly displayed;
* the correct HORIZON logo is used;
* the HORIZON background is restored;
* the finish flag is visible;
* desktop and mobile layouts are verified;
* no console errors remain;
* no failed local asset requests remain;
* all automated and browser-level tests pass.

If something fails:

FIX IT → TEST AGAIN.

Continue until the complete site works.

---

# 1. FIRST INSPECT THE CURRENT REPOSITORY

Before changing anything:

* inspect all HTML;
* inspect all CSS;
* inspect all JavaScript;
* inspect `assets/`;
* inspect `data/`;
* inspect Firebase integration;
* inspect current translations;
* inspect the currently available logo and background images;
* inspect the participant photos;
* inspect route files;
* inspect current map markers.

Do NOT invent new filenames if existing appropriate assets already exist.

IMPORTANT:

I have intentionally deleted files/images that contained NORTHLINE branding.

Therefore:

**DO NOT recreate, restore or reference deleted NorthLine image files.**

If an old code reference points to a NorthLine asset that no longer exists:

REMOVE THAT REFERENCE.

Replace it with the correct HORIZON asset that currently exists in the repository.

The repository itself is the source of truth for available images.

---

# 2. RESTORE HORIZON VISUAL IDENTITY

The current site looks too basic and generic.

It has become overly minimal after the refactor.

HORIZON should feel:

* cinematic;
* premium;
* Alpine;
* serious;
* expedition-oriented;
* atmospheric;
* distinctive;
* visually connected to the HORIZON poster.

Do NOT turn it into:

* a SaaS dashboard;
* a corporate website;
* a plain dark Bootstrap layout;
* a generic black/white landing page.

The aesthetic should use:

* deep navy;
* charcoal;
* warm ivory;
* antique gold;
* muted orange sunset accents;
* subtle Alpine imagery;
* atmospheric gradients;
* light grain / texture where tasteful;
* stronger typography hierarchy.

---

# 3. RESTORE THE HERO BACKGROUND IMAGE

The homepage previously had a strong background image.

It is currently missing.

There is an existing HORIZON background image in the repository.

Inspect the current available assets, including:

```text
data/Sfondo.png
```

and any newer/moved equivalent.

Use the correct CURRENT HORIZON background asset.

DO NOT use any NorthLine background.

The homepage hero should again have a cinematic Alpine background.

Recommended structure:

```text
background image
+
dark navy overlay
+
subtle gradient
+
text
```

The image should remain visible but must not reduce text readability.

Example conceptual CSS:

```css
background:
  linear-gradient(
    180deg,
    rgba(5, 12, 20, .55),
    rgba(5, 12, 20, .92)
  ),
  url(...);
```

Do not blindly copy these exact values.

Tune them visually.

The hero must feel large and immersive.

---

# 4. CORRECT HORIZON LOGO ONLY

The navigation currently uses an incorrect or outdated logo treatment.

The HORIZON website must use ONLY HORIZON branding.

DO NOT use:

* NorthLine logo;
* NorthLine mountain graphic;
* NorthLine favicon;
* deleted NorthLine image assets.

Inspect the repository and identify the CURRENT correct HORIZON logo/favicon.

Use it consistently for:

* navbar;
* favicon;
* mobile header;
* social metadata where appropriate.

If there are conflicting old favicon/logo files, determine which is the actual HORIZON version from the existing assets and remove obsolete references.

Do NOT generate a new logo unless absolutely necessary.

Use the current HORIZON asset already present.

---

# 5. PARTICIPANT SECTION — DESKTOP LAYOUT

The participant images are vertical portraits.

The current layout crops the subject too aggressively because the image is forced into a wide card format.

On DESKTOP:

use a horizontal participant layout.

For each participant:

```text
IMAGE | TEXT
```

or alternating:

```text
Damiano:
IMAGE | TEXT

Nicolas:
TEXT | IMAGE
```

if visually balanced.

Preferred layout:

* image approximately 35–40% width;
* text approximately 60–65%;
* image remains portrait;
* image height around 420–520 px depending on viewport;
* preserve the subject;
* avoid aggressive cropping.

Use:

```css
object-fit: cover;
```

only if the correct `object-position` preserves the person.

If necessary use:

```css
object-fit: contain;
```

or a portrait wrapper with a dark background.

The primary requirement is:

**DO NOT CUT OFF THE SUBJECT OF THE PARTICIPANT PHOTO.**

Use the existing participant images currently present in the repository:

```text
DamZan.jpg
NicMos.jpeg
```

or their current moved equivalents.

On MOBILE:

stack:

```text
IMAGE
TEXT
```

while preserving a reasonable portrait ratio.

---

# 6. MAKE THE VISUAL LANGUAGE LESS MONOTONOUS

The current site uses too much:

```text
white heading
white paragraph
dark background
card
dark background
white heading
```

This feels flat.

Introduce stronger hierarchy.

Use the HORIZON accent colour selectively.

For example:

### Eyebrows / section labels

warm antique gold.

### Important heading words

Use accent colour selectively.

Example:

```html
<h2>
  500 kilometres.
  <span class="accent">One direction.</span>
</h2>
```

Do NOT colour every heading.

### Bold emphasis

Use `<strong>` strategically inside paragraphs for:

* 500 km;
* 10,000 m+;
* two people;
* east to west;
* Piz Chavalatsch;
* Chancy.

Do not overuse bold.

### Section backgrounds

Introduce subtle variation:

```text
transparent
dark navy
slightly lighter navy
background image section
route section
full-width statement
```

rather than putting everything inside identical cards.

### Dividers

Use subtle lines / glow / route-like motifs.

The site should feel like an expedition story, not a collection of dashboard widgets.

---

# 7. TRANSLATIONS MUST BE COMPLETE

THIS IS A HIGH PRIORITY REQUIREMENT.

If the user chooses:

```text
Deutsch
```

then the ENTIRE visible website must be German.

Not:

```text
Navbar German
Heading English
Button English
Footer English
Status German
```

The same applies to:

* English;
* Italian;
* German;
* French.

Audit EVERY visible string.

This includes:

* navbar;
* page titles;
* hero;
* CTA buttons;
* countdown;
* statistics labels;
* map controls;
* status;
* accessibility labels;
* Project page;
* Journey page;
* Replay page;
* Dashboard;
* Progress;
* Diary;
* Timeline;
* modal labels;
* empty states;
* errors;
* buttons;
* footer;
* photo descriptions if static;
* date labels;
* countdown labels.

Use ONE translation system.

Prefer:

```html
data-i18n="..."
```

for static UI.

Use:

```js
t('...')
```

for dynamic strings.

DO NOT leave hardcoded English UI inside page HTML.

---

# 8. LANGUAGE SELECTOR ON EVERY PAGE

Currently language switching is not consistently available from desktop on all pages.

Fix this.

Every public page must have the SAME desktop language selector in the header.

Every public page must also have language switching inside the mobile navigation.

Pages include at minimum:

```text
index.html
live.html
project.html
gallery.html
replay.html
dashboard.html
progress.html
diary.html
timeline.html
```

The selected language must persist when navigating.

Example:

User:

```text
Home → Deutsch
```

then clicks:

```text
Live
```

Live must already be German.

Then:

```text
Project
```

Project must still be German.

Use the shared:

```text
languageStorageKey
```

configuration.

Also update:

```html
<html lang="de">
```

dynamically when German is selected.

Equivalent for:

```text
en
it
fr
```

---

# 9. COUNTDOWN — ADD THE ACTUAL START DATE

Above the countdown, display the expedition start date clearly.

The configured expedition start is:

```text
31 August 2026
04:00
```

using the ISO configuration as the single source of truth.

Do NOT hardcode separate dates in every language.

Display something visually like:

```text
START
31 AUGUST 2026 · 04:00
```

Localized properly.

German:

```text
START
31. AUGUST 2026 · 04:00
```

Italian:

```text
PARTENZA
31 AGOSTO 2026 · 04:00
```

French:

```text
DÉPART
31 AOÛT 2026 · 04:00
```

English:

```text
START
31 AUGUST 2026 · 04:00
```

Use `Intl.DateTimeFormat` where appropriate.

Then display:

```text
DAYS
HOURS
MINUTES
SECONDS
```

below.

This should make it immediately obvious what the countdown refers to.

The countdown must actually work.

---

# 10. LIVE MAP — MUST LOAD

The map is still a critical requirement.

The page must never remain indefinitely at:

```text
LOADING MAP…
```

Test:

* Leaflet initialization;
* map element dimensions;
* route file;
* tile layer;
* route parsing;
* map fitting;
* markers;
* Firebase data;
* page-specific JS load order.

Verify actual network requests.

If any route file produces a 404, fix the canonical path.

Use ONE route URL from config.

---

# 11. RETURN THE WAVING FINISH FLAG

Restore the animated finish flag at CHANCY.

The route ending must visually show a small waving flag marker.

Requirements:

* placed at the real finish coordinate;
* Chancy / west;
* visible without obscuring the route;
* stylistically compatible with HORIZON;
* subtle animation;
* not cartoonish;
* accessible title / tooltip.

The animation should resemble a fabric flag gently moving in wind.

For example:

```text
small pole
small ivory/gold or Swiss-inspired flag
subtle CSS wave animation
```

Do NOT use an unrelated NorthLine flag asset.

Prefer CSS/SVG if no appropriate current image exists.

The marker should appear on:

* live route map;
* other route maps where endpoint markers are shown.

Start marker should remain clearly distinguishable.

---

# 12. FIREBASE SIMULATION DATA IS CURRENTLY NOT VISIBLE

There is currently test activity data in Firebase.

It was imported intentionally to simulate a real activity and verify the website.

The data is not being displayed correctly.

You MUST investigate the REAL Firebase payload.

Do NOT guess its structure.

Inspect the response from the configured Firebase endpoint.

Current configuration references:

```text
/livetrack/points.json
```

Determine what Firebase actually returns.

Possible structures may include:

```js
{
  id1: {...},
  id2: {...},
  id3: {...}
}
```

or:

```js
[
  {...},
  {...}
]
```

or nested activity objects.

The current parser is too simplistic if it only expects a single direct point.

Implement a robust parser.

---

# 13. FIREBASE TRACK DATA NORMALIZATION

Create a proper normalization layer.

For example:

```js
normalizeTrackPayload(payload)
```

that returns:

```js
[
  {
    latitude,
    longitude,
    timestamp,
    altitude,
    speed,
    heartRate,
    ...
  }
]
```

sorted chronologically.

Support the REAL imported structure.

Do not blindly support arbitrary formats that do not exist.

Inspect actual data first.

Validate coordinates.

Reject invalid entries.

Sort timestamps.

Deduplicate points where appropriate.

---

# 14. DO NOT ONLY FETCH THE LAST POINT

For the simulation and live journey the application needs TWO concepts:

### Latest live point

Used for:

* current marker;
* current speed;
* altitude;
* last update.

### Full recorded track

Used for:

* travelled path;
* distance completed;
* charts;
* replay;
* statistics;
* elevation;
* route progress.

Implement separate functions if appropriate:

```js
fetchLiveTrack()
fetchLatestLivePoint()
```

Do not attempt to build the complete site around only one GPS point.

---

# 15. SHOW THE RECORDED ACTIVITY ON THE MAP

The Firebase simulation track should become visible as a second line.

The map should distinguish:

```text
PLANNED HORIZON ROUTE
```

and:

```text
ACTUAL RECORDED TRACK
```

Recommended visual treatment:

Planned route:

```text
muted ivory / dashed / slightly transparent
```

Actual track:

```text
strong warm gold / orange / solid
```

Do not use unrelated bright colours.

Current position should be at the end of the actual track.

---

# 16. CALCULATE PROGRESS FROM THE ACTUAL TRACK

Using Firebase recorded data, calculate:

* distance covered;
* distance remaining;
* current progress;
* elapsed time;
* current altitude;
* elevation gain;
* current speed;
* last update;
* estimated arrival where meaningful.

Do not leave:

```text
--
```

if the value can be calculated.

If a metric genuinely cannot be calculated:

display a translated unavailable state.

---

# 17. IMPORTANT — TRACK PROGRESS SHOULD FOLLOW THE ROUTE

Do not calculate progress only using straight-line distance between Piz Chavalatsch and current location.

Use route progress.

Prefer projecting the current/actual track onto the planned HORIZON route.

Then calculate how far along the planned route the user is.

This is much more meaningful than simple crow-fly distance.

---

# 18. STATUS MUST MATCH REAL DATA

Currently the public Live page can show:

```text
LIVE · Updated 1 min ago
```

even while the map does not display the corresponding activity.

Fix this.

Status must come from the real track state.

Examples:

### No points

```text
NOT STARTED
```

### Recent valid point

```text
LIVE
```

### Latest point older than threshold

```text
SIGNAL DELAYED
```

### Explicit pause/rest

```text
RESTING
```

### Finished

```text
FINISHED
```

Use the configured:

```text
staleDataThresholdMs
```

where appropriate.

---

# 19. HOMEPAGE LIVE PREVIEW

If Firebase contains valid simulation/live data, the homepage statistics should also update.

For example:

```text
distance
remaining
progress
time
elevation
estimated arrival
```

The homepage and Live page must consume the same calculated data.

Do NOT implement two different calculation systems.

---

# 20. HEADER CONSISTENCY

Use one shared header/navigation design across all public pages.

It should contain:

```text
HORIZON LOGO
HORIZON
page subtitle where appropriate

HOME
LIVE
PROJECT
JOURNEY
REPLAY

LANGUAGE SELECTOR
```

Desktop language switcher must always be visible.

Do not hide language switching only because the user is on another page.

---

# 21. NAVBAR VISUAL IMPROVEMENT

The navbar currently feels functional but generic.

Refine it subtly.

Suggested qualities:

* slight transparency;
* backdrop blur;
* thin warm divider;
* active link accent;
* smooth hover underline;
* HORIZON logo clearly visible;
* compact language selector.

Do not make it oversized.

---

# 22. SECTION TYPOGRAPHY

Improve typography hierarchy.

Use approximately:

```text
EYEBROW
small uppercase
wide letter-spacing
warm gold

H1
very large
ivory

H2
large
ivory + optional accent word

BODY
soft ivory/grey
comfortable line-height
```

Important numbers should be stronger.

For example:

```text
500 KM
10,000 M+
2 PEOPLE
```

should feel visually significant.

---

# 23. PROJECT PAGE LANGUAGE AND CONTENT

Project currently contains old solo-oriented language such as:

```text
personal test
personal challenge
```

Update this.

HORIZON is undertaken by TWO people together.

The Project page should consistently describe:

* shared challenge;
* two participants;
* one route;
* shared pacing;
* shared decisions;
* east-to-west crossing.

Then translate the COMPLETE page into:

```text
EN
IT
DE
FR
```

---

# 24. PHOTO / PARTICIPANT DESIGN

Use the real participant photos as meaningful visual content.

Do not crop them into anonymous squares.

Desktop cards should feel editorial.

Example:

```text
┌─────────────────────────────────────────────┐
│  PORTRAIT        NAME                      │
│  PORTRAIT        TAGLINE                   │
│  PORTRAIT        biography...              │
│  PORTRAIT        biography...              │
└─────────────────────────────────────────────┘
```

Use subtle border/texture.

Do not make every element another identical card.

---

# 25. ADD MORE VISUAL RHYTHM

Introduce visually distinct sections.

Possible ideas:

* hero with photo;
* clean countdown;
* large stats strip;
* dark narrative section;
* participants with large portraits;
* route section with map silhouette;
* full-width statement;
* challenge grid;
* live CTA.

Use whitespace intentionally.

Avoid a repetitive sequence of identical rectangles.

---

# 26. DO NOT OVERDESIGN

Even though the current site is too basic, do not overcorrect.

Avoid:

* neon;
* glowing everything;
* excessive animations;
* glassmorphism everywhere;
* excessive gradients;
* huge border radii;
* generic tech-dashboard styling.

HORIZON should remain:

```text
serious
physical
outdoor
premium
Swiss
cinematic
```

---

# 27. ACCESSIBILITY MUST REMAIN GOOD

While improving visuals:

* preserve contrast;
* preserve keyboard navigation;
* preserve focus states;
* preserve alt text;
* preserve semantic headings;
* preserve reduced-motion support.

For the waving flag:

respect:

```css
@media (prefers-reduced-motion: reduce)
```

and disable/reduce animation.

---

# 28. TEST ALL FOUR LANGUAGES ON ALL PAGES

Create a test matrix.

For EVERY page:

```text
EN
IT
DE
FR
```

Check visible text.

There must be zero accidental English strings when German is selected, except proper names such as:

```text
HORIZON
Piz Chavalatsch
Chancy
Damiano Zanoni
Nicolas Mooser
```

The same rule applies to other languages.

---

# 29. TEST DESKTOP PARTICIPANT LAYOUT

At least test:

```text
1366 × 768
1440 × 900
1920 × 1080
```

Verify participant images are not badly cropped.

Check faces/head/body composition.

Use appropriate:

```css
object-position
```

per participant if needed.

Do not force both images to use the same crop if the source composition differs.

---

# 30. TEST MOBILE PARTICIPANT LAYOUT

Test:

```text
375 × 667
390 × 844
430 × 932
```

Images should stack above text.

No horizontal overflow.

Do not make portraits excessively tall.

---

# 31. IMAGE PATH CLEANUP

Because obsolete NorthLine image files were intentionally deleted:

search the complete repository for:

```text
northline
NorthLine
north-line
old logo filenames
deleted image references
```

For EVERY result:

determine whether it is legitimate.

If it references obsolete visual branding:

REMOVE OR REPLACE IT.

Do not recreate missing old images just to eliminate a 404.

Use the current HORIZON images.

---

# 32. SOCIAL PREVIEW

Also inspect:

```text
og:image
twitter:image
preview.png
social-card.png
```

Ensure they visually belong to HORIZON.

If any existing social preview still uses NorthLine branding, replace the reference with a current HORIZON asset.

Do not use a deleted NorthLine file.

---

# 33. FULL FIREBASE TEST

Use the current simulation data already present in Firebase.

Test:

1. fetch succeeds;
2. payload is parsed;
3. track contains points;
4. points appear on map;
5. final/latest point appears;
6. statistics update;
7. actual route is drawn;
8. replay can consume the points;
9. dashboard can consume the points;
10. stale/live status works.

If the simulation data has unexpected structure:

adapt the parser to the real structure.

Do NOT modify the Firebase data just to make your existing parser pass unless the data itself is clearly malformed.

The website should adapt to the intended data model.

---

# 34. DEBUG NETWORK REQUESTS

Inspect browser network.

There should be no unexplained:

```text
404
403
CORS
500
failed JSON
failed image
failed JS
failed CSS
```

especially for:

```text
Firebase
GeoJSON
GPX
background
logo
participant images
Leaflet tiles
```

---

# 35. TEST THE LIVE MAP AFTER FIREBASE DATA LOADS

The following must be simultaneously visible:

* planned HORIZON route;
* actual activity track;
* latest/current position;
* finish flag at Chancy.

Then test:

* center current position;
* center visitor position;
* fullscreen;
* resize;
* mobile.

---

# 36. UPDATE ROUTE ENDPOINT VISUALS

Use:

### Start

A clear start marker.

### Finish

The waving flag.

Tooltip/popups should use localized content.

Example DE:

```text
START · OSTEN
Piz Chavalatsch
```

```text
ZIEL · WESTEN
Chancy
```

Equivalent in EN/IT/FR.

---

# 37. RESTORE BACKGROUND WITHOUT HURTING PERFORMANCE

Optimize background loading.

Use:

* modern compression if possible;
* reasonable resolution;
* preload only if necessary;
* correct sizing;
* no 10 MB hero background.

Do not remove the background again merely to improve Lighthouse.

Visual identity matters.

Optimize it instead.

---

# 38. FINAL VISUAL REVIEW

Before completion compare the final site against these questions:

### Does this feel like HORIZON?

If the logo were removed, could it still visually be recognized as the same expedition brand?

### Is the homepage memorable?

Or is it simply dark cards and white text?

### Are important ideas visually emphasized?

* east to west;
* 500 km;
* Piz Chavalatsch;
* Chancy;
* two people;
* live journey.

### Does every section have a reason to exist?

Remove unnecessary repetition.

---

# 39. FINAL FUNCTIONAL TEST

After all styling changes:

TEST FUNCTIONALITY AGAIN.

Visual changes must not break:

* countdown;
* translations;
* Firebase;
* map;
* markers;
* fullscreen;
* mobile nav;
* stats;
* replay;
* gallery;
* dashboard.

---

# 40. FINAL ACCEPTANCE CONDITIONS

You may only stop when ALL are true:

* HORIZON logo correct;
* no NorthLine branding;
* background restored;
* participant photos visible and correctly cropped;
* desktop participant image/text layout improved;
* site no longer visually monotonous;
* headings have stronger hierarchy;
* selective accent colour added;
* selective bold emphasis added;
* all pages fully translated;
* desktop language selector available everywhere;
* language persists between pages;
* countdown date visible;
* countdown works;
* map loads;
* route loads;
* finish flag restored;
* Firebase imported activity data visible;
* actual track visible;
* statistics derived from Firebase;
* navigation consistent;
* no broken image references;
* no console errors;
* no unexplained failed requests;
* desktop tested;
* mobile tested;
* all automated tests pass.

If ANY item is not true:

DO NOT DECLARE COMPLETION.

Continue fixing.

---

# FINAL REPORT FORMAT

When the implementation is truly finished provide:

## VISUAL CHANGES

What was restored/improved.

## BRANDING

Which HORIZON assets are now used.

## TRANSLATIONS

Pages and languages tested.

## FIREBASE

Actual payload structure discovered and how it is parsed.

## MAP

Planned route, actual track, live point and finish flag validation.

## RESPONSIVE

Desktop/mobile sizes tested.

## TESTS

Automated and browser tests run.

## ERRORS

Console/network errors remaining.

Only write:

**ALL ACCEPTANCE TESTS PASS**

if every requirement above actually passed.
