# HORIZON — COMPLETE WEBSITE AUDIT, REFACTOR, BUG FIXING AND RELEASE VALIDATION

You are working on the complete HORIZON website repository.

Live production website:

https://damzan06.github.io/HorizonWeb/

Repository:

https://github.com/DamZan06/HorizonWeb

Your task is NOT to fix one isolated bug.

Your task is to perform a **complete end-to-end audit, repair, refactor and validation of the entire website** until the project behaves like a stable production website.

## ABSOLUTE COMPLETION RULE

Do **not** declare the task finished after making code changes.

Do **not** declare the task finished because the code “looks correct”.

Do **not** declare the task finished because one page works.

Do **not** stop after fixing the bugs explicitly mentioned in this prompt.

You may declare the work finished ONLY when:

1. every public page has been tested;
2. every interactive function has been tested;
3. every map has been tested;
4. every timer/countdown has been tested;
5. every data-driven statistic has been tested;
6. every language switcher has been tested;
7. every navigation link has been tested;
8. desktop and mobile layouts have been tested;
9. browser console errors have been eliminated;
10. broken network requests and 404s have been eliminated;
11. all automated tests pass;
12. the website still works after a fresh reload;
13. failure/empty-data states behave correctly;
14. no feature remains permanently stuck on placeholders such as `--`, `Loading…`, `Calculating…`, etc. unless there is legitimately no available data;
15. no legacy/dead code is required for the new implementation to function.

If something fails during testing, fix it and run the tests again.

Repeat:

**AUDIT → FIX → TEST → FIND REMAINING PROBLEMS → FIX → TEST AGAIN**

until the complete website passes.

---

# 1. FIRST: UNDERSTAND THE ENTIRE PROJECT

Before editing anything, inspect the complete repository.

Inspect at minimum:

* every `.html` file;
* every `.js` file;
* every `.css` file;
* `package.json`;
* `firebase-config.js`;
* route GPX/GeoJSON files;
* `assets/`;
* `data/`;
* `tools/`;
* navigation;
* Firebase/live tracking integration;
* translations;
* status logic;
* map logic;
* chart logic;
* gallery logic;
* replay logic;
* admin functions;
* progress/timeline/diary functions.

Do not assume a file is still used just because it exists.

Determine:

* which files are current;
* which files are legacy;
* which functions are duplicated;
* which HTML files load obsolete scripts;
* which scripts are never loaded;
* which assets have incorrect paths;
* which DOM elements expect controllers that do not exist;
* which controllers are implemented but never imported.

Create a dependency map internally before making large changes.

---

# 2. IMPORTANT KNOWN BUG — HOMEPAGE COUNTDOWN

The homepage currently displays:

* Days `--`
* Hours `--`
* Minutes `--`
* Seconds `--`
* `Calculating remaining time...`

and stays like this.

THIS IS A BUG.

The expedition start configuration currently contains approximately:

```js
startDateIso: '2026-08-31T04:00:00+02:00'
```

The countdown therefore does not require any external API.

Implement a reliable countdown controller.

The countdown must:

* use the configured `startDateIso`;
* calculate the difference using `Date.now()`;
* respect the explicit timezone contained in the ISO timestamp;
* update immediately when the page initializes;
* update once per second;
* correctly calculate days;
* correctly calculate remaining hours;
* correctly calculate minutes;
* correctly calculate seconds;
* never show negative values;
* work after changing language;
* not reset when translations are applied;
* cleanly transition when the countdown reaches zero.

Before departure:

```text
05 DAYS
18 HOURS
23 MINUTES
14 SECONDS
```

or the equivalent layout currently used by the website.

After departure, replace the countdown state with a meaningful expedition state such as:

`HORIZON IS UNDERWAY`

or use the existing translated equivalent.

Do not permanently leave:

`Calculating remaining time...`

That text may only exist momentarily before JavaScript initializes.

Create a dedicated controller if necessary, for example:

```text
js/pages/home.js
```

Do NOT bury this feature inside a giant legacy runtime file.

Test the calculation using mocked dates both before and after departure.

---

# 3. CRITICAL BUG — LIVE MAP

The Live page currently displays:

`LOADING MAP…`

and the map does not reliably load.

Investigate the complete cause.

A known inconsistency currently exists:

`js/pages/live.js` tries to load:

```text
data/route/horizon-route.geojson
```

while the repository currently contains:

```text
data/horizon-route.geojson
```

Fix this properly.

DO NOT simply hardcode another random path.

There must be ONE canonical route path stored in configuration.

For example:

```js
routeGeoJsonUrl: 'data/horizon-route.geojson'
```

Then all maps use that value.

Search the entire repository for:

```text
Horizon.gpx
horizon.gpx
horizon-route.geojson
data/route/
data/Horizon.gpx
```

and eliminate inconsistent paths.

The Live map must successfully display:

* Leaflet base tiles;
* the complete HORIZON route;
* correct geographic orientation;
* route start;
* route finish;
* live position when real data exists;
* useful empty state before departure;
* visitor position when permission is granted;
* center-on-user control;
* center-on-live-position control;
* fullscreen control.

If live GPS data does not yet exist because HORIZON has not started, THAT MUST NOT BREAK THE MAP.

The planned route must still display.

Correct behavior before departure:

```text
Map visible
Planned route visible
Status: Expedition not started / waiting for live position
No fake live marker
```

Incorrect behavior:

```text
LOADING MAP…
forever
```

Also make sure that failures are not hidden.

The current architecture must not do something like:

```js
Promise.allSettled(...).finally(() => setMapStatus('Map ready'))
```

if the route actually failed.

The UI must distinguish:

* map ready;
* route unavailable;
* tile service unavailable;
* live data unavailable;
* geolocation denied.

---

# 4. REMOVE LEGACY SCRIPT REFERENCES

Audit every HTML file for obsolete JavaScript references.

There are currently pages that still reference:

```html
<script src="script.js"></script>
```

while the architecture is being moved to:

```text
js/app.js
js/config.js
js/map.js
js/firebase.js
js/pages/*
js/ui/*
```

Determine whether `script.js` is legacy.

If the new architecture replaces it:

REMOVE EVERY REFERENCE TO IT.

Do not keep compatibility files “just in case”.

Also fix `package.json`.

The current test script must NOT keep checking only:

```text
script.js
```

when the application actually consists of many JavaScript modules.

---

# 5. AUTOMATED JAVASCRIPT VALIDATION

Create a script that syntax-checks EVERY JavaScript file.

For example, create an appropriate Node test utility that recursively scans:

```text
js/
tools/
```

and validates all `.js` files.

`npm test` must fail if any application JavaScript contains a syntax error.

Do not manually list every JavaScript file if a recursive solution is reasonable.

---

# 6. ADD REAL BROWSER END-TO-END TESTING

The current smoke tests are not sufficient for this website.

Introduce browser-level testing, preferably **Playwright**, if suitable for this static GitHub Pages project.

The purpose is NOT to overengineer the website.

The purpose is to prevent future refactors from silently breaking maps, timers, controls and navigation again.

Add an E2E test suite.

It should launch the local server and test the real pages in Chromium.

At minimum test:

```text
index.html
live.html
project.html
gallery.html
replay.html
dashboard.html
progress.html
diary.html
timeline.html
```

and admin functionality where safely testable.

Tests must detect:

* uncaught JavaScript errors;
* console errors;
* failed local resource requests;
* 404 scripts;
* 404 CSS;
* 404 images;
* missing route data;
* controls that do nothing;
* permanent loading states.

---

# 7. HOMEPAGE COMPLETE FUNCTIONAL TEST

Test everything on `index.html`.

Verify:

### Navigation

* logo → Home
* Home
* Live
* Project
* Journey
* Replay
* mobile hamburger
* mobile overlay
* Escape closes mobile navigation
* clicking a link closes mobile navigation

### Languages

Test:

* English
* Italian
* German
* French

Switch languages repeatedly.

Refresh the page.

The selected language must persist using the configured storage key.

No section should remain accidentally in another language.

### Countdown

Test it thoroughly as defined earlier.

### Hero

Verify both CTA links work.

### Live status

Before departure it must NOT claim that HORIZON is live.

The website currently knows the configured departure date.

Use the same shared expedition-state calculation everywhere.

### Statistics

Before departure, display deliberate zero/empty states.

Do not show misleading information.

Example:

```text
Distance: 0 km
Remaining: 500 km
Completed: 0%
Elapsed: 0 h
Elevation: 0 m
Steps: 0
ETA: —
```

or an equally clear presentation.

Avoid arbitrary `--` when a value can logically be determined.

### Content

Remove unnecessary repetition.

The homepage currently repeats several concepts multiple times:

* east → west;
* 500 km;
* 10,000 m;
* moving toward the horizon;
* endurance;
* the challenge.

Keep the cinematic storytelling, but make the page tighter.

Do NOT remove the strong HORIZON identity.

---

# 8. CORRECT ROUTE ORIENTATION

HORIZON goes:

```text
PIZ CHAVALATSCH → CHANCY
EAST → WEST
RIGHT → LEFT on a normal north-up map
```

Check every graphical representation of the route.

Do not accidentally render Piz Chavalatsch on the left and Chancy on the right.

The geographic map must always remain:

```text
north = top
east = right
west = left
```

Never mirror the route for aesthetic reasons.

---

# 9. LIVE PAGE COMPLETE FUNCTIONAL TEST

Test `live.html`.

Verify:

* page initialization;
* navigation;
* language switcher;
* current status;
* Firebase request;
* latest live point;
* route GeoJSON;
* map;
* route geometry;
* current marker;
* distance;
* remaining distance;
* completion;
* speed;
* altitude;
* last update;
* progress bar;
* elapsed time;
* elevation gain;
* steps;
* visitor distance.

Test controls:

* center visitor;
* center live position;
* fullscreen;
* exit fullscreen;
* browser resize after fullscreen;
* denied geolocation;
* unsupported geolocation.

Test BEFORE expedition data exists.

Test WITH mock live data.

Test stale live data.

Test malformed live data.

Test Firebase unavailable.

The page must never pretend:

`LIVE · Updated 1 min ago`

when no recent valid point actually exists.

Use the same centralized state logic everywhere.

---

# 10. FIREBASE / LIVE TRACKING

Audit `js/firebase.js` completely.

Validate:

* URL;
* expected payload format;
* latest point selection;
* timestamp parsing;
* latitude;
* longitude;
* altitude;
* speed;
* heart rate if available;
* handling of missing fields;
* ordering;
* duplicate points;
* empty database;
* network failures;
* stale data.

Do not let malformed historical points crash current rendering.

All data parsing should be isolated from presentation.

---

# 11. CENTRAL EXPEDITION STATE

Create ONE function responsible for determining the expedition state.

For example:

```text
not-started
live
delayed
resting
finished
offline
```

Do NOT independently infer this state in five different files.

Possible input:

```js
getExpeditionState({
  now,
  startDate,
  latestPointTimestamp,
  forcedAdminState
})
```

Home, Live, Dashboard and other pages must consume the same state.

This prevents contradictions such as:

Homepage:

`Not started`

while Live says:

`LIVE · Updated 1 min ago`

at exactly the same time.

---

# 12. DASHBOARD — MAKE IT ACTUALLY WORK

`dashboard.html` already contains UI for:

* distance covered;
* distance remaining;
* completion;
* current speed;
* average moving speed;
* average total speed;
* maximum speed;
* altitude;
* elevation;
* total time;
* moving time;
* heart rate;
* average heart rate;
* calories;
* estimated water loss;
* ETA.

It also contains charts for:

* speed;
* altitude;
* heart rate;
* cumulative elevation.

At the moment the page does not correctly connect all of the required controllers.

Create:

```text
js/pages/dashboard.js
```

or another clean equivalent.

Load the correct dependencies.

Use:

```text
js/firebase.js
js/stats.js
js/charts.js
```

where appropriate.

Do not duplicate calculations inside dashboard.js if they belong in stats.js.

Test every metric.

If a metric cannot currently be calculated because data does not contain what is needed, explicitly display:

```text
Not available
```

or an equivalent translated state.

Do NOT invent values.

### Charts

Verify:

* Chart.js loads;
* canvases are found;
* datasets populate;
* no chart is initialized twice;
* X-axis distance mode works;
* X-axis time mode works;
* fullscreen buttons work;
* exiting fullscreen works;
* charts resize correctly;
* empty datasets show a proper empty state.

---

# 13. GALLERY / JOURNEY — MAKE EVERY FEATURE REAL

Audit `gallery.html`.

The page currently contains UI for:

* gallery grid;
* photo map;
* fullscreen map;
* image modal;
* previous image;
* next image;
* close modal;
* title;
* location;
* description.

Make ALL of it functional.

Create a page controller:

```text
js/pages/gallery.js
```

if appropriate.

Implement:

* loading images;
* rendering cards;
* geolocated image markers;
* marker clustering;
* clicking a photo;
* clicking a marker;
* modal opening;
* modal closing;
* previous;
* next;
* keyboard arrows;
* Escape;
* appropriate focus behavior;
* fullscreen map.

Replace the current fullscreen button text:

```text
?
```

with a proper accessible icon consistent with the Live map.

Replace simplistic:

```text
X
《
》
```

where appropriate with accessible SVG icons.

Ensure every button has:

* `type="button"`;
* accessible label;
* keyboard support;
* visible focus state.

If there are no journey photos yet, display a polished empty state.

It must not look broken.

---

# 14. REPLAY — IMPLEMENT THE CONTROLS

`replay.html` contains:

* Play;
* Pause;
* Reset;
* speed slider;
* status;
* map.

Make them actually work.

Create:

```text
js/pages/replay.js
```

if necessary.

The replay should:

1. load the actual recorded points;
2. initialize the route/map;
3. start at the first real point;
4. advance chronologically;
5. move a marker;
6. update status;
7. pause reliably;
8. resume reliably;
9. reset reliably;
10. change playback speed without corrupting state;
11. stop at the final point.

Prevent multiple timers from being created when Play is clicked repeatedly.

Before recorded journey data exists, do NOT show controls pretending a replay is ready.

Instead show a clear state:

```text
Replay will become available once journey data has been recorded.
```

Disable controls when no replay exists.

---

# 15. PROJECT PAGE

Review `project.html`.

The project is no longer framed as a solo challenge.

HORIZON is completed by TWO people travelling together.

Remove old language such as:

```text
personal test
personal challenge
```

when it implies only one participant.

Make the narrative consistent with:

```text
two people
one route
east to west
shared endurance
shared decisions
shared journey
```

Keep the tone serious and premium.

Avoid overdramatic generic motivational language.

---

# 16. DIARY

Test `diary.html`.

Verify:

* data source;
* loading;
* chronological order;
* date formatting;
* language;
* empty state;
* photos;
* text;
* links;
* mobile layout.

If the diary and gallery overlap conceptually, establish clear responsibilities.

Example:

```text
Journey / Gallery = photographs and places
Diary = written chronological field notes
```

Do not keep redundant pages without clear purpose.

---

# 17. TIMELINE

Test `timeline.html`.

Every timeline element must either:

* load real content;
* show a deliberate empty state;
* or be removed if obsolete.

Do not leave half-implemented pages accessible publicly.

---

# 18. PROGRESS PAGE

Test `progress.html`.

If it contains badges/milestones:

* define how they are unlocked;
* centralize milestone configuration;
* derive unlocked state from real distance/elevation/data;
* handle zero-data state correctly;
* test boundary conditions.

Example:

```text
49.99 km → 50 km badge locked
50.00 km → unlocked
```

Do not hardcode state independently in HTML.

---

# 19. ADMIN PAGE

Audit `admin.html`.

Check:

* authentication/session behavior;
* forced expedition status;
* forms;
* localStorage;
* Firebase write operations if any;
* empty values;
* validation;
* user feedback;
* accidental public exposure of sensitive credentials.

IMPORTANT:

Never put secret Firebase admin credentials or private API keys in client-side JavaScript.

If the page only changes browser-local state, make that obvious.

Make sure admin controls cannot accidentally make public visitors see fake expedition status unless this is intentionally backed by shared storage/database.

---

# 20. NAVIGATION — ONE SOURCE OF TRUTH

There must be ONE canonical public navigation configuration.

Do not manually maintain a different navbar inside every HTML page.

The visible main public navigation should remain simple.

Recommended:

```text
HOME
LIVE
PROJECT
JOURNEY
REPLAY
```

Advanced telemetry can be linked from Live instead of making the navbar overcrowded.

Progress can similarly be linked contextually if appropriate.

The navigation component must correctly set:

```html
aria-current="page"
```

for the current page.

Test desktop and mobile.

---

# 21. LANGUAGE SYSTEM — ONE IMPLEMENTATION

There are currently signs of duplicated translation logic between the newer `i18n.js` architecture and legacy runtime code.

Consolidate this.

Use ONE translation catalog.

Use ONE language persistence mechanism.

Prefer:

```html
data-i18n="..."
```

for static content and:

```js
t('...')
```

for dynamic content.

Do not use:

* one translation system in Home;
* another in Live;
* DOM-selector-specific translation hacks;
* a third translation dictionary inside legacy runtime.

All four supported languages must work:

```text
English
Italian
German
French
```

Test every public page in every language.

No mixed-language page.

No broken accents.

No untranslated placeholder UI.

---

# 22. CSS REFACTOR

The repository currently contains both the legacy monolithic stylesheet and the newer CSS structure.

Complete the migration.

Target structure:

```text
css/
├── tokens.css
├── global.css
├── home.css
├── live.css
├── dashboard.css
├── gallery.css
├── replay.css
├── project.css
└── ...
```

Do not create page CSS files if a page needs only a few global styles.

Avoid unnecessary fragmentation.

### `tokens.css`

Centralize reusable design values:

```css
--color-*
--font-*
--space-*
--radius-*
--shadow-*
--transition-*
--content-width-*
--navbar-*
--breakpoint-* where useful
```

Do not repeatedly hardcode the same colors, spacing and radii throughout the project.

### Remove inline style attributes

Replace things such as:

```html
style="display:grid;gap:16px;"
style="min-height:560px;"
```

with meaningful CSS classes.

### Delete legacy stylesheet

ONLY after all pages have been visually verified with the new CSS structure:

remove `style.css`.

Before deleting it, search for every class still dependent on it.

---

# 23. UI/UX QUALITY PASS

After functionality is fixed, audit the UI.

The visual direction should remain:

* dark;
* premium;
* Alpine;
* expedition-focused;
* minimal;
* cinematic;
* warm ivory/gold accent;
* strong typography;
* restrained.

Do NOT redesign HORIZON into a generic SaaS dashboard.

However, improve weak UI.

### Avoid excessive cards

Currently too many sections are presented as similarly styled rectangular cards.

Create stronger visual hierarchy.

The most visually important areas should be:

1. hero;
2. route/live map;
3. people/journey.

Secondary information can be visually quieter.

### Reduce excessive homepage repetition

Shorten copy where the same message appears repeatedly.

Every section should answer a different question.

Recommended narrative:

```text
HERO
What is happening?

LIVE STATUS
What is happening right now?

THE CHALLENGE
What is HORIZON?

THE SCALE
How large is it?

THE PEOPLE
Who is doing it?

THE ROUTE
Where does it go?

WHAT MAKES IT HARD
Why is it difficult?

FIELD NOTES
What is happening during the journey?

FINAL CTA
Follow it live.
```

Do not repeat essentially identical motivational paragraphs.

---

# 24. RESPONSIVE TESTING

Test at least:

```text
375 × 667
390 × 844
430 × 932
768 × 1024
1024 × 768
1366 × 768
1440 × 900
1920 × 1080
```

Verify:

* navbar;
* hero;
* countdown;
* cards;
* long German/French strings;
* maps;
* map buttons;
* fullscreen;
* charts;
* gallery;
* modal;
* replay controls;
* footer.

No horizontal scrolling.

No overlapping content.

No clipped buttons.

No tiny unreadable labels.

---

# 25. ACCESSIBILITY PASS

Check:

* semantic headings;
* one sensible `h1` per page;
* button labels;
* links;
* skip link;
* form labels;
* language select;
* `aria-expanded`;
* `aria-current`;
* focus indicators;
* modal focus;
* Escape behavior;
* keyboard-only navigation;
* contrast;
* images and alt text;
* status updates.

Do not abuse `aria-live`.

For example a second-by-second countdown should not cause a screen reader to announce four changing numbers every second.

---

# 26. PERFORMANCE

Measure obvious performance problems.

Especially maps.

The Live page should not unnecessarily load:

* GPX parser if using GeoJSON;
* MarkerCluster if not needed;
* Chart.js when there are no charts;
* gallery libraries on Live;
* legacy runtime code.

Only load page-specific dependencies on pages that need them.

Prefer the prebuilt:

```text
horizon-route.geojson
```

for rendering rather than parsing the GPX client-side on every visit.

The GPX remains the geographic source file, while GeoJSON can be the optimized web artifact.

Make sure GeoJSON is generated from GPX and not manually edited inconsistently.

If appropriate:

```text
npm run build:route
```

should regenerate it.

---

# 27. REMOVE `runtime.js` LEGACY MONOLITH

`js/runtime.js` is currently far too large and duplicates responsibilities now present elsewhere.

Do NOT leave the website dependent on two parallel architectures.

Extract useful functionality into clearly scoped modules.

Suggested architecture:

```text
js/
├── app.js
├── config.js
├── i18n.js
├── firebase.js
├── stats.js
├── status.js
├── map.js
├── charts.js
│
├── ui/
│   └── navigation.js
│
└── pages/
    ├── home.js
    ├── live.js
    ├── dashboard.js
    ├── gallery.js
    ├── replay.js
    ├── diary.js
    ├── progress.js
    └── admin.js
```

This is a guide, not an obligation.

Do not create pointless 10-line files just to follow this structure.

Main principle:

**one responsibility → one obvious module.**

When all needed functionality has been migrated and tested:

DELETE `runtime.js`.

---

# 28. DATA AND ASSET ORGANIZATION

Clean the repository.

`data/` should contain actual application data.

Images should not live beside GPX/GeoJSON files.

Recommended:

```text
assets/
├── people/
├── journey/
├── backgrounds/
├── icons/
├── favicons/
└── social/
```

and:

```text
data/
├── Horizon.gpx
├── horizon-route.geojson
└── other structured data
```

or:

```text
data/route/
```

IF YOU CHOOSE `data/route/`, then actually move the files there and update EVERY reference.

Do not create paths in JavaScript pointing to folders that do not exist.

---

# 29. CENTRAL CONFIGURATION

Keep stable project values centralized.

Examples:

```js
projectName
expeditionYear
startDateIso
startLocation
finishLocation
expectedDistanceKm
expectedElevationM
routeGeoJsonUrl
routeGpxUrl
firebaseURL
staleDataThresholdMs
supportedLanguages
defaultLanguage
languageStorageKey
```

Do not rewrite:

```text
500
10000
Piz Chavalatsch
Chancy
31 August
```

in application logic in multiple places.

Textual storytelling may mention them, but calculations must use configuration.

Do not put transient runtime state inside config.

---

# 30. NETWORK ERROR AUDIT

Using browser developer tools / Playwright request listeners, verify that normal page loads do NOT produce unnecessary:

```text
404
403
500
CORS errors
failed script requests
failed stylesheet requests
failed route requests
```

A page is NOT considered passing while its console/network panel contains expected-looking but unresolved errors.

Fix the root cause.

---

# 31. JAVASCRIPT CONSOLE AUDIT

Every page must be tested with console collection enabled.

Fail the test on:

```text
console.error
pageerror
uncaught exception
unhandled promise rejection
```

Review `console.warn` as well.

Do not simply suppress console messages.

Fix the underlying problem.

---

# 32. EMPTY STATE TESTING

HORIZON has not necessarily started yet.

Therefore empty data is a NORMAL application state, not an exception.

Every data page needs a designed empty state.

Examples:

### Live

```text
Expedition not started.
The planned route is shown below.
Live position will appear after departure.
```

### Dashboard

```text
Telemetry will appear once tracking begins.
```

### Gallery

```text
Field notes will appear during the journey.
```

### Replay

```text
Replay becomes available after recorded tracking data exists.
```

The site should still look complete and intentional before the expedition starts.

---

# 33. TEST WITH MOCK DATA

A major requirement:

Do NOT wait for the real expedition to test live functionality.

Create mock fixtures representing:

### State A — no data

```text
No Firebase points
Before start date
```

### State B — first point

One valid GPS point.

### State C — active journey

Multiple valid points containing:

* timestamps;
* latitude;
* longitude;
* altitude;
* speed;
* HR when available.

### State D — stale tracker

Latest point older than the stale threshold.

### State E — malformed point

Missing or invalid coordinate.

### State F — finished journey

Last point at/near finish or explicitly completed state.

Run UI logic against these fixtures.

---

# 34. TEST CALCULATIONS

Unit test critical pure calculations.

At minimum:

### Countdown

* before start;
* exactly at start;
* after start.

### Distance remaining

```text
remaining = total - completed
```

Clamp between `0` and total.

### Completion

Clamp between:

```text
0%
100%
```

### Elevation

Reject impossible/invalid points where necessary.

### Moving speed

Correctly distinguish zero/missing movement.

### ETA

Do not calculate nonsensical ETA from:

* zero speed;
* one point;
* stale data;
* invalid data.

### Status

Test all expedition states.

---

# 35. FINAL TEST MATRIX

Before declaring completion, explicitly run this matrix:

| Page      | Desktop | Mobile | Navigation | Language | Main function | Empty state | Errors |
| --------- | ------- | ------ | ---------- | -------- | ------------- | ----------- | ------ |
| Home      | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |
| Live      | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |
| Project   | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |
| Journey   | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |
| Replay    | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |
| Dashboard | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |
| Progress  | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |
| Diary     | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |
| Timeline  | PASS    | PASS   | PASS       | PASS     | PASS          | PASS        | 0      |

Do not write PASS unless it was actually tested.

---

# 36. FINAL CLEANUP

Before finishing, search the entire repository for:

```text
TODO
FIXME
TEMP
temporary
placeholder
Calculating
LOADING
script.js
runtime.js
data/route/
console.log
style="
href="#"
?
```

Review every result.

Not every occurrence is necessarily wrong, but none should remain unexplained.

Also find:

* unused files;
* unused functions;
* unused CSS selectors;
* duplicate constants;
* obsolete event handlers;
* libraries loaded on pages where they are not needed.

Remove dead code.

---

# 37. FINAL ACCEPTANCE TEST

Perform one final clean test from scratch.

1. Stop the development server.
2. Start it again.
3. Open a new browser context.
4. Clear localStorage/sessionStorage.
5. Load Home.
6. Verify countdown.
7. Navigate using the actual navbar.
8. Open Live.
9. Verify the map.
10. Verify planned route.
11. Test live empty state.
12. Test mock live data.
13. Open Project.
14. Open Journey.
15. Test gallery/modal/map.
16. Open Replay.
17. Test replay or correct unavailable state.
18. Open Dashboard.
19. Test metrics/charts.
20. Test Progress.
21. Test Diary.
22. Test Timeline.
23. Switch EN → IT → DE → FR.
24. Refresh pages.
25. Test mobile.
26. Review console.
27. Review failed network requests.
28. Run all automated tests.

ONLY IF ALL OF THIS PASSES may you finish.

---

# 38. WHAT YOU MUST REPORT WHEN FINISHED

Do not give me a vague response such as:

> “I fixed several issues and improved the architecture.”

Instead provide:

## FIXED

Exact bugs repaired.

## REFACTORED

Files/modules moved or reorganized.

## REMOVED

Legacy/dead files removed.

## TESTS ADDED

List of automated tests.

## MANUAL/E2E VALIDATION

Pages and functionality verified.

## REMAINING LIMITATIONS

Only list genuinely unavoidable limitations caused by external services.

For each limitation explain why it cannot be fixed in this repository.

## FINAL RESULT

Only write:

**ALL ACCEPTANCE TESTS PASS**

if they actually do.

Otherwise continue working.

---

# MOST IMPORTANT INSTRUCTION

Do not optimize for finishing quickly.

Optimize for leaving me with a website where I no longer have to manually discover that:

* the countdown does not count;
* the map does not load;
* a button does nothing;
* a chart is empty because its controller was never imported;
* an old script path produces a 404;
* one page says “Live” while another says “Not started”;
* a refactor silently disconnected a feature.

Treat this as a production release.

**Do not stop after editing. Test the result. If something is broken, keep fixing it. Only declare completion once the entire HORIZON website has passed the complete validation process.**
